apiVersion: 1

# /var/lib/grafana/dashboards に置いた JSON を自動で読み込む。
providers:
  - name: sample2
    orgId: 1
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
